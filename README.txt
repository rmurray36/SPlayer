Experimentation with AngularJS example.
SermonPlayer is the Angular UI - use a web server (or >browser-sync start --server --files .)
AudioPlayerRS is the REST web service in NodeJS Express  - use npm install the npm start to start server